name: SpringBootFun
environment: test
spring:
  profiles:
    active: integration-test
  datasource:
    url: jdbc:postgresql://localhost:5433/infinitesound_test
    username: infinitesounduser
    password: infinitesoundpassword
  jpa:
    database: POSTGRESQL
    show-sql: true
#    database-platform: org.hibernate.dialect.PostgreSQLDialect
#    generate-ddl: true


